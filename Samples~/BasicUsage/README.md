# Basic Usage

1. Import this sample via Package Manager.
2. Add the `DLoggerBasicUsage` component to any GameObject in a scene.
3. Enter Play mode.
4. Open **Window > DraasGames > Console** and try the level toggles, tag filter and search.

To configure the minimum log level and tag list, open **Project Settings > DraasGames > DLogger**.
